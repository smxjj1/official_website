---
description: official_website 默认使用 main 分支提交与推送
alwaysApply: true
---

# Git 分支约定

- 本仓库默认分支是 `main`，不是 `master`。
- 提交、推送、开 PR 一律针对 `main`（或基于 `main` 的功能分支）。
- 不要切换到 `master`，也不要向 `origin/master` 推送，除非用户明确要求同步 `master`。
- 开始提交前先执行 `git branch --show-current`，确认当前是 `main`；若不是，先 `git checkout main`。
- 远程推送使用：`git push origin main` 或 `git push -u origin HEAD`（仅在已位于 `main` 时）。
